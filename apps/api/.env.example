# Copier en apps/api/.env — NE JAMAIS COMMITTER .env
DATABASE_URL="postgresql://bozochat:bozochat@localhost:5432/bozochat"
# Origine autorisée (CORS + WebSocket). En prod : https://<votre-app>.up.railway.app
APP_ORIGIN="http://localhost:5173"
PORT=3000
NODE_ENV=development
# Base dédiée aux tests (les tests vident les tables !)
TEST_DATABASE_URL="postgresql://bozochat:bozochat@localhost:5432/bozochat_test"
